name: First Action

on: workflow_dispatch
jobs:

  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World"
      - name: Print Goodbye
        run: echo "Done - bye!"
      - name: multi line command
        run: |
          echo "First Output"
          echo "Second Output"
